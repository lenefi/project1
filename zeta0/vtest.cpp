#include <iostream>
#include <ofstream>
#include <cmath>
#include "zeta0.h"


void vtest (int n, int s, int k)

pi= M_PI;
outfile ("error.txt");

	for (i=1; i<=k; i++){
	n= std::pow(2,k)
	pi_n = zeta0(n,s);
	err = abs(pi-pi_n);

outfile
}
}
